{
  "name": "libreria-azul",
  "version": "1.0.0",
  "private": true,
  "description": "Tienda y catálogo administrable de Librería Azul",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check app.js && node --check admin.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0"
  }
}
